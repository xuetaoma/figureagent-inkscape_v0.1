[project]
name = "inkscape-copilot"
version = "0.1.0"
description = "AI-assisted Inkscape publication-figure editing agent."
requires-python = ">=3.10"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = ["inkscape_copilot"]
